# HANDOFF — 파서 v2 설계 문서군 & 발표 시스템 확장

> 인수인계 문서. 이 세션에서 진행한 두 갈래 작업(① 반도체 설비 로그파서 개선 프로젝트의 설계 문서군, ② 그 문서를 발표자료로 만들기 위한 스크롤형 발표 시스템의 확장)의 현재 상태·위치·결정사항·후속을 정리한다.
>
> 최종 갱신: 2026-07-04 · 작업 브랜치: `claude/parser-v2-docs-nnbjum` (전 작업 `main`에 머지 완료, PR #1~#6)

---

## 0. 한눈에

- **무엇**: 레거시 로그파서를 책임 분리된 새 구조로 재설계하는 프로젝트의 설계 문서군을 아키텍처/계약 레벨로 작성했고, 이를 발표자료로 찍어내기 위한 발표 시스템(이 레포)의 간극을 적대적 리뷰로 찾아 확장 스펙 + 파일럿을 만들었다.
- **원칙**: 모든 설계 문서는 **아키텍처/계약 레벨의 골격**이다. 세부 구현(컬럼·필드·알고리즘)은 변경 가능성이 커서 의도적으로 비워 뒀다 — 실제 값은 사내에서 채운다.
- **상태**: 5대 컴포넌트 중 4개 설계서 완료, Configuration 파서만 미작성(차후 고도화 영역). 발표 시스템 확장 스펙 + 파일럿 1편 완료.

---

## 1. 프로젝트 컨텍스트 (도메인)

반도체 설비 **이벤트 로그파서** 개선. BI/DW ETL이 아니라 설비 실시간 로그 → 정형 데이터 가공 파이프라인.

- **컴포넌트(책임 분리)**: 스케줄러 / 파일 수집기 / 컨버터(비표준→표준) / 표준 로그 파서(값 정규화·보정·병합 내부 포함) / Configuration 파서
- **폴리글랏**: 파서·컨버터 = **.NET**(CPU 바운드), 스케줄러·수집기·Config파서 = **Python**(개발 속도)
- **인프라**: 온프렘 AP 서버(설비 근처) → 공정 DB 서버. TSV 실시간 로그. **S3/parquet/Spark 아님.**
- **핵심 도메인 개념**: 표준 로그 스펙(TSV), start/end 병합, **carryover**(파일경계 이월), 값 정규화·보정(50종+), 기준정보 마스터, live/rework/따라잡기 시나리오, 설비별 진행상태 DB(SSOT), 골든 파일 동등성 테스트
- **운영(고도화)**: Docker화 + Airflow(오케스트레이션·catchup) + n8n(이벤트·알림) + 큐/워커(부하 분산). 단 **부하 평준화·CPU 기반 동적 분배는 커스텀** — Airflow가 대체 못 함.

---

## 2. 문서 위치 (§2.1–2.3 전부 `reference/parserimprove/uploads/`)

> ⚠️ 2026-07-04 재구조: 발표 시스템이 `core/` + `styles/indigo-serif/`로 분리됐고 `parserimprove/`는 `reference/`로 이관됨. 아래 경로는 그에 맞춰 갱신.

### 2.1 프로젝트 설계 문서군
| 문서 | 내용 | 상태 |
|------|------|------|
| `parser_project_revised.md` | 프로젝트 전체(상위 문서, 정본) | 기존 |
| `file_collector_design.md` | 파일 수집기 — 원본 확보·가용성, 시나리오 불가지 | ✅ |
| `scheduler_design.md` | 스케줄러 — 상태(SSOT)/정책/메커니즘 3계층, 시나리오, CPU 부하평준화 | ✅ |
| `converter_design.md` | 컨버터 — 비표준→표준 **형식 변환만** | ✅ |
| `standard_parser_design.md` | 표준 파서 — 단일 파사드, 내부 정규화·보정·병합·carryover | ✅ |
| `integration_contracts_design.md` | 컴포넌트 간 통합·인터페이스 계약(폴리글랏 경계·인코딩·TSV·타임존 등) | ✅ |
| **Configuration 파서** | — | ❌ 미작성(차후) |

### 2.2 모노레포 문서군 리뷰 & 골격
| 문서 | 내용 |
|------|------|
| `monorepo_docs_review.md` | 사용자가 만든 DW ETL 템플릿 문서군의 적대적 리뷰(5 페르소나) — "DW 템플릿 이식, 도메인 부적합" 판정 |
| `monorepo_skeleton/` | 그 결론대로 재편한 도메인 문서군 골격(11→6 + 용어집, 도메인 계약 대상) |

### 2.3 발표 시스템 확장 & 파일럿
| 문서 | 내용 |
|------|------|
| `spec_extensions.md` | 발표 시스템 확장 스펙(색법칙 결정·새 컴포넌트·도식·페이지타입·mermaid매핑·문서군 규칙·토큰 거버넌스) |
| `converter_pilot.dc.html` | `converter_design.md`를 실제 발표자료로 만든 파일럿(실행 가능) |

### 2.4 발표 시스템 스펙 라이브러리 (이 레포의 본체 — 스타일별 분리)
- **`core/`** (스타일 불가지): `runtime-spec.md`(셸·JS·DOM계약·서빙, HEX 없음).
- **`styles/indigo-serif/`** (현 스타일 SSOT): `design.md`(토큰·컴포넌트·도식) · `authoring-guide.md`(보이스·구조·페이지타입, 미분할) · `composition-guide.md`(밀도) · `design.tokens.md`(토큰 미러) · `template.dc.html`(스켈레톤+support.js) · `design-system.answerkey.dc.html`(정답지).
- **`README.md`**: 진입점 + 스타일 레지스트리.

캐노니컬 예시: `reference/parserimprove/로그파서 개선 설명자료 v2.dc.html`.

---

## 3. 핵심 결정사항

1. **폴리글랏 OK** — 컴포넌트 경계가 프로세스/파일/DB로만 넘으므로 .NET/Python 혼용 가능. 계약(인코딩·TSV·타임존·job 페이로드)을 단일 소스로 관리해야 어긋나지 않음.
2. **컴포넌트 간 통신** — MVP는 CLI spawn + 파일 + DB(A 모델). 고도화는 gRPC/메시지 큐(B 모델). raw 소켓 직접구현 금지. DB를 SSOT로 유지, 소켓 상태를 이중 진실로 두지 않음.
3. **스케줄러 ≠ dataset DAG** — "수천 설비 팬아웃 + CPU 부하평준화 + live/rework/따라잡기". backfill과 "따라잡기"는 다른 개념.
4. **모노레포 문서군** — 사용자 초안(DW ETL 템플릿)은 도메인 부적합 → 메타 원칙 4개만 계승하고 도메인 계약(표준로그스펙·기준정보·진행상태·골든)으로 재작성.
5. **발표 시스템 색법칙** — 모노-휴 유지(색=old/new+상태만). out-of-scope는 중립 그레이(앰버/그린 금지), 폴리글랏 범주는 색 아닌 mono-tag 뱃지. (`spec_extensions.md §0`)
6. **발표 가능성 판정** — 프로젝트 브리프 형은 현 스펙으로 v2 수준 재현 가능. 컴포넌트/계약 문서는 신규 컴포넌트·페이지타입 없이는 즉흥→드리프트. 그 간극을 `spec_extensions.md`가 채움.

---

## 4. 후속 작업 (우선순위)

### 프로젝트 문서
- [ ] **표준 로그 스펙(TSV) 컬럼 확정** — 가장 핵심 단일 소스. 사내 진행 예정.
- [ ] DB 결과/상태/기준정보 스키마 확정
- [ ] Configuration 파서 설계서(차후 고도화)
- [ ] rework 재적재 방식·carryover 저장 방식(상위 문서 §10 미정)

### 발표 시스템
- [x] **다중 스타일 폴더 분리** (2026-07-04): `core/`(불가지) + `styles/indigo-serif/`(SSOT) + `reference/`. 적대적 5-리뷰어 채점(3.44/10, NO_GO) → 축소 실행: 런타임 크롬색 토큰화 + rename + 스타일 레지스트리. authoring 분할·support.js dedup은 2번째 스타일 시로 연기.
- [ ] `reference/parserimprove/uploads/spec_extensions.md`를 코어/스타일 스펙(styles/indigo-serif/design.md·authoring-guide.md·composition-guide.md)에 실제 병합
- [ ] 토큰 CSS 변수화 + allowlist 린터 + range→단일값 확정, 캐노니컬 v2 오프토큰(`#A8AEC4`·`#C0A8E0`) 정정
- [ ] `converter_pilot.dc.html`을 **브라우저 렌더링해 밀도/레이아웃 육안 검증**(현재는 정적 정합성만)
- [ ] `integration_contracts_design.md`로 **두 번째 파일럿** 제작 → N×N 매트릭스·계약 카드·체크리스트 실사용 검증
- [ ] SVG 엣지 예외는 `support.js`의 `cssToObj` data-URI 맹글링 수정이 선행돼야 개방(그 전엔 매트릭스로 근사)

---

## 5. 작업 이력 (PR)

| PR | 내용 |
|----|------|
| #1 | 파일 수집기 설계서 |
| #2 | 스케줄러 설계서 |
| #3 | 통합·인터페이스 계약 |
| #4 | 모노레포 리뷰 + 재편 골격 |
| #5 | 컨버터 + 표준 로그 파서 설계서 |
| #6 | 발표 시스템 확장 스펙 + 컨버터 파일럿 |

> 리뷰는 서브에이전트/워크플로우로 오케스트레이션했고, 본 세션은 총괄·저작·병합을 맡았다. 모든 설계 문서는 "세부 구현은 변경 가능하므로 골격만" 원칙을 지킨다 — 실제 값은 사내에서 채운다.
