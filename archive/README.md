# archive/

Frozen historical material. **Not part of the presentation-system product** — kept only for
provenance. Nothing here carries live TODOs or authoritative spec.

| Path | What |
|------|------|
| `parserimprove/` | Semiconductor log-parser design-doc set + its `.dc.html` renders. The original design reference the `indigo-serif` style specs were first abstracted from. Unrelated to this repo's actual work. |
| `HANDOFF-parserimprove.md` | Handoff doc from the parser-project session (covers the parser docs and the multi-style restructure that split `core/` + `styles/`). Superseded by `README.md` / `CLAUDE.md` + git history. |
